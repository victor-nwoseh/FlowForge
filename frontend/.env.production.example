# =============================================================================
# FlowForge Frontend - Production Environment Configuration
# =============================================================================
# Copy this file to .env.production and fill in the values
# These values are embedded at BUILD TIME (not runtime)
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------

# Backend API URL (full URL including /api prefix)
# This is where the frontend makes API requests
# Examples:
#   - Same domain: /api (uses relative path, requires proxy)
#   - Different domain: https://api.flowforge.example.com/api
#   - Railway: https://flowforge-backend-production.up.railway.app/api
#   - Render: https://flowforge-backend.onrender.com/api
VITE_API_URL=https://your-backend-domain.com/api

# -----------------------------------------------------------------------------
# WebSocket Configuration
# -----------------------------------------------------------------------------

# WebSocket URL for real-time execution updates
# Should point to the backend server (Socket.io endpoint)
# Examples:
#   - Same domain: (leave empty, uses relative connection)
#   - Different domain: wss://api.flowforge.example.com
#   - Railway: wss://flowforge-backend-production.up.railway.app
#   - Render: wss://flowforge-backend.onrender.com
# Note: Use wss:// for HTTPS deployments, ws:// for HTTP
VITE_WS_URL=wss://your-backend-domain.com

# -----------------------------------------------------------------------------
# Build Configuration (Optional)
# -----------------------------------------------------------------------------

# Enable/disable source maps in production (default: false)
# VITE_SOURCEMAP=false

# Analytics tracking ID (optional)
# VITE_GA_TRACKING_ID=G-XXXXXXXXXX
